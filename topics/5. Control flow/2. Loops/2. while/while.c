#include <stdio.h>

int main (){
    int i = 0;              // initialization
    while (i < 5){          // condition
        printf("%d", i);
        i++;                // update
    }
    return 0;
}